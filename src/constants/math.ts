import { BN } from 'utils/helpers'

export const BN_ZERO = BN(0)
export const BN_ONE = BN(1)
const BN_ZERO_ONE = BN(0.1)

// MRC-98 Community Pool burn amount (300M MARS tokens)
export const MRC_98_BURN_AMOUNT = 300_000_000

export const MIN_AMOUNT = 0.000001
const MAX_AMOUNT_DECIMALS = 6

const MILLISECONDS_PER_MINUTE = 60000
